# API base only — never put HACKATHON_ACCESS_CODE here.
#
# Local Vite against production (usual for viewing seeded runs):
VITE_HACKATHON_API_BASE=https://admin.coretechs.se/hackathon
#
# Local Vite against local Django (webeditor on :8000):
# VITE_HACKATHON_API_BASE=http://127.0.0.1:8000/hackathon
#
# Also set the same var in Vercel → Project → Settings → Environment Variables.
